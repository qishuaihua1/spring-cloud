server:
  port: 8008
spring:
  application:
    name: eureka-center
eureka:
  instance:
    hostname: eureka-server-03
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://eureka-server-01:8888/eureka/,http://eureka-server-02:8088/eureka/
  server:
    enable-self-preservation: false

